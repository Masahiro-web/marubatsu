{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --omit=dev",
        "buildCommand": "npm run vercel-build"
      }
    }
  ]
}
